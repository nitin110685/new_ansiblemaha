---
- hosts: all
  tasks: 
    - name: i want to install tree
      apt:
       name: tree
       state: present

    - name: i want to install webserver on wget
      apt:
       name: wget
       state: present

    - name: i want to install webserver on ubuntu nodes
      apt:
       name: apache2
       state: present
       